/** @type {import("prettier").Config} */
const config = {
    plugins: ["prettier-plugin-astro"],
    printWidth: 100,
    semi: true,
    singleQuote: false,
    tabWidth: 4,
    trailingComma: "all",
};

export default config;
